# Zaleznosci dla Windows — bez gphoto2 i pyusb (libgphoto2/libusb nie
# istnieja na tej platformie; aparat obsluguje digiCamControl przez HTTP,
# patrz WINDOWS.md).
pillow>=11
rich>=14
numpy>=2
scipy>=1.15
rembg>=2.0.60
onnxruntime>=1.20
requests>=2.32
python-dotenv>=1.0
pywebview>=5
