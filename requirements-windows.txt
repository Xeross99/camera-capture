# Zaleznosci dla Windows — bez gphoto2 i pyusb (libgphoto2/libusb nie
# istnieja na tej platformie; aparatem steruje Canon EDSDK przez ctypes,
# patrz WINDOWS.md).
pillow>=11
rich>=14
numpy>=2
scipy>=1.15
rembg>=2.0.60
# -directml zamiast zwyklego onnxruntime: inferencja rembg na GPU przez
# DirectX 12 (kazda karta — NVIDIA/AMD/Intel, zero instalowania CUDA);
# provider wybiera _gpu_providers() w src/background.py, CLEAN_BG_GPU=false
# wymusza CPU. Na maszynie bez sprawnego GPU DirectML sam spada na CPU.
onnxruntime-directml>=1.20
requests>=2.32
python-dotenv>=1.0
pywebview>=5
# ramie RoArm-M2-S po USB-serial (src/robot.py); ciagnie pyserial.
# Windows potrzebuje jeszcze sterownika CH343 do plytki ramienia.
roarm-sdk>=0.1.0
